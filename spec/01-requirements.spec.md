# Requirements
FR: simulated home state; natural-language commands; agent/MCP startup registration; capability routing; security domain; environment domain; energy analysis; multi-agent orchestration; A2A collaboration; MCP tools/resources/prompts; post-action verification; AG-UI event streaming.

NFR: containers; env configuration; service-name networking; health/readiness; structured logs; correlation IDs; explicit failures; no secrets; deterministic mock mode for tests.
Non-goals: real hardware, voice, mobile app, production IoT, contracts/legal features.
